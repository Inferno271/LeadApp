<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.WRITE_CALL_LOG"/>
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />




    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <!-- Разрешения для API уровнем 26 и выше -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />
    <!-- Конец разрешений для API уровнем 26 и выше -->

    <!-- Разрешения для API уровнем менее 26 -->
    <uses-permission-sdk-23 android:name="android.permission.READ_CALL_LOG" />
    <uses-permission-sdk-23 android:name="android.permission.WRITE_CALL_LOG" />
    <!-- Конец разрешений для API уровнем менее 26 -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LeadApp15"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CallPopupActivity"
            android:theme="@style/AppTheme">
            <!-- Другие настройки активности -->
        </activity>

        <service
            android:name=".CallPopupService"
            android:enabled="true"
            android:exported="false">

            <!-- Добавьте разрешение на использование службы в режиме Foreground -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </service>

        <receiver
            android:name=".CallReceiver"
            android:enabled="true"
            android:exported="false">
            <!-- Фильтры намерений (Intent Filters) -->
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
                <action android:name="android.intent.action.NEW_OUTGOING_CALL" />
                <!-- Другие фильтры намерений, если необходимо -->
            </intent-filter>
        </receiver>
    </application>

</manifest>